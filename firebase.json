{
  "_comment": "Hosting is intentionally NOT configured here: this site is served by GitHub Pages (see CNAME). Deploy with 'firebase deploy --only functions,firestore:rules' — never a bare 'firebase deploy', which would try to touch Hosting too.",
  "firestore": {
    "rules": "firestore.rules"
  },
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": ["node_modules", ".git", "firebase-debug.log", "firebase-debug.*.log"]
    }
  ]
}
